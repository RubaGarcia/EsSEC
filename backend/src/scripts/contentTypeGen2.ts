/* import { generate } from 'contentful-typescript-codegen';
import path from 'path';

// Variables de entorno con tus credenciales de Contentful
const contentfulSpaceId = 'YOUR_SPACE_ID';
const contentfulEnvironmentId = 'YOUR_ENVIRONMENT_ID'; // generalmente es 'master'
const contentfulAccessToken = 'YOUR_CDA_ACCESS_TOKEN';

generate({
    spaceId: contentfulSpaceId,
    environmentId: contentfulEnvironmentId,
    accessToken: contentfulAccessToken,
    outputFile: path.resolve(__dirname, 'AutoGenTypes2.d.ts'), // Ruta del archivo de salida
}).then(() => {
    console.log('Tipos de Contentful generados correctamente!');
}).catch((error) => {
    console.error('Error al generar los tipos:', error);
});
 */